---
title: "Installation — @piplup/rhf-core"
description: Learn how to install the core and optional adapter packages for Piplup.
---

## Install Core Package

Install the core package:

```package-install
@piplup/rhf-core
```

Optionally, install the adapters package for pre-built integrations:

```package-install
@piplup/rhf-adapters
```